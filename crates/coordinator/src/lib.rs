//! Worker registration, parked polling, leases, capacity, and completion for
//! one active runtime. Distributed gossip and cross-node routing are outside
//! the community boundary.

#[derive(Debug, Default)]
pub struct Coordinator;
